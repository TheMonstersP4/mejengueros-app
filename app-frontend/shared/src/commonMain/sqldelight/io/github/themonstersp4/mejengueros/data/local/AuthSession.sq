CREATE TABLE AuthSession (
    id INTEGER NOT NULL PRIMARY KEY CHECK (id = 1),
    username TEXT NOT NULL
);

selectSession:
SELECT username
FROM AuthSession
WHERE id = 1;

upsertSession:
INSERT OR REPLACE INTO AuthSession(id, username)
VALUES (1, ?);

clearSession:
DELETE FROM AuthSession;

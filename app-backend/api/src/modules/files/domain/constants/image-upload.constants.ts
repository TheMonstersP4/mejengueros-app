/**
 * Maximum request-level profile image size accepted by validation and config.
 */
export const PROFILE_IMAGE_HARD_MAX_BYTES = 8 * 1024 * 1024;

/**
 * Default profile image size limit used when no environment override is set.
 */
export const PROFILE_IMAGE_DEFAULT_MAX_BYTES = 5 * 1024 * 1024;

/**
 * Representative profile image size used in public API documentation examples.
 */
export const PROFILE_IMAGE_EXAMPLE_BYTES = 512 * 1024;
